# Required — Claude API key for intent classification and agent execution
ANTHROPIC_API_KEY=sk-ant-...

# Google Workspace export (optional — pick ONE auth method)
# Option 1: Service account JSON file path
# GOOGLE_SERVICE_ACCOUNT_FILE=./service-account.json
# Option 2: Inline service account JSON
# GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# Google Drive folder to place exported documents (optional)
# GOOGLE_DRIVE_FOLDER_ID=1abc...

# Gradio sharing (optional)
# GRADIO_SHARE=1           # Set to 1 for a temporary public link
# GRADIO_SERVER_PORT=7860   # Default port
